apiVersion: v1
kind: Service
metadata:
  name: gold-rate-service
spec:
  type: NodePort
  selector:
    app: gold-rate-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080